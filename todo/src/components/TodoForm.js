import React from 'react';

function TodoForm() {
    return (
        <form>
            <input type='text' />
            <button>Add Item</button>
        </form>
    );
};

export default TodoForm;